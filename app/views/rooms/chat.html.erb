<div class="container">
  <!--中断確認モーダル-->
  <div class="modal quit-modal" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title">対戦を中断しますか？</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
          <p>相手に確認を取らずに対戦を即終了します。</p>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-dismiss="modal">閉じる</button>
          <%= link_to '中断', accept_room_path(@room), method: :post, class: "btn btn-outline-danger" %>
        </div>
      </div>
    </div>
  </div>
  
  <div class="row">
    <h1 class="offset-md-2 col-md-7">
      <%= @room.title %> ：対戦チャットルーム
      <% if @room.status == 3 || @room.status == 5 %>
        ログ（メッセージ送信不可）
      <% end %>
    </h1>
  </div>
  <div class="row">
    <div class="chat offset-md-2 col-md-8">
      <%= render 'messages/message', room: @room, messages: @messages, message: @message %>
    </div>
  </div>
  <div class="row">
    <div class="finishing offset-md-7 d-flex">
      <% if @room.status == 2 %>
        <div class="finish-room mr-2">
          <%= render 'rooms/finish-room', room: @room %>
        </div>
        <div>
          <button class="btn btn-outline-danger quit-btn">中断</button>
        </div>
      <% end %>
    </div>
  </div>
</div>





